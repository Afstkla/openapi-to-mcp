openapi: "3.0.0"
info:
  title: Simple API
  version: "1.0.0"
paths:
  /items:
    get:
      operationId: listItems
      summary: List all items
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  properties:
                    id:
                      type: integer
                    name:
                      type: string
